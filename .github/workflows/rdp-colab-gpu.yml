name: RDP Colab GPU

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run VPS Script
        run: |
          chmod +x aztutools
          ./aztutools start
          sleep infinity
